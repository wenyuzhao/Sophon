[workspace]
members = [
    "kernel",
    "proton",
    "boot/uefi",
    "init",
    "drivers/emmc"
]

[profile.dev]
panic = "abort"
# opt-level = 1

[profile.release]
debug = true
panic = "abort"
# lto = true