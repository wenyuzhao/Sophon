[workspace]
members = [
    "kernel",
    "proton",
    "arch/aarch64",
    "arch/aarch64-uefi",
    "arch/x86_64-uefi",
    "init",
    "drivers/emmc"
]

[profile.dev]
panic = "abort"
# opt-level = 1

[profile.release]
debug = true
panic = "abort"
# lto = true