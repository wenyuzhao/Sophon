[workspace]
members = [
    "sophon",
    "libs/heap",
    "libs/ipc",
    "tools/initfs_builder",
    "boot/uefi",
    "user/init",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
debug = true
lto = true
codegen-units = 1